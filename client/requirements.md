## Packages
framer-motion | Complex animations for the ocean theme
date-fns | Formatting dates for documents
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Auth is handled via Replit Auth (/api/login, /api/logout).
Documents API available at /api/documents.
Use Playfair Display for headings and Inter for body text (configure in index.css).
Theme: Deep Ocean (Navy, Teal, Gold).
